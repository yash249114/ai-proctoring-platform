import { useState } from "react";

export default function QuestionForm({ onSubmit, initialData, loading }) {
  // Not used standalone — question creation is done inline in CreateQuestion.jsx
  return null;
}
